---
title: "Official Plugins"
description: "Production-ready plugins maintained by the INK team"
---

Official plugins are fully supported, regularly updated, and guaranteed to work with the latest INK version.

## Social Plugins

### @ink/plugin-twitter

Full Twitter integration for posting, replying, and monitoring.

```bash
npm install @ink/plugin-twitter
```

```typescript
import { twitterPlugin } from "@ink/plugin-twitter";

const agent = new Agent({
  plugins: [
    twitterPlugin({
      // Required credentials
      apiKey: process.env.TWITTER_API_KEY,
      apiSecret: process.env.TWITTER_API_SECRET,
      accessToken: process.env.TWITTER_ACCESS_TOKEN,
      accessSecret: process.env.TWITTER_ACCESS_SECRET,

      // Behavior settings
      postEnabled: true,
      replyEnabled: true,
      minHealthToPost: 50,
      cooldownMinutes: 30,

      // Content filters
      maxLength: 280,
      bannedWords: ["shill", "guaranteed"],
    }),
  ],
});
```

**Features:**
- Post tweets from agent thoughts
- Reply to mentions
- Monitor keywords
- Thread support
- Media attachments

**Actions:**
- `twitter.post(content)` - Post a tweet
- `twitter.reply(tweetId, content)` - Reply to a tweet
- `twitter.like(tweetId)` - Like a tweet
- `twitter.getMentions()` - Get recent mentions

---

### @ink/plugin-discord

Discord bot integration for community engagement.

```bash
npm install @ink/plugin-discord
```

```typescript
import { discordPlugin } from "@ink/plugin-discord";

const agent = new Agent({
  plugins: [
    discordPlugin({
      token: process.env.DISCORD_BOT_TOKEN,
      guildId: process.env.DISCORD_GUILD_ID,
      channelId: process.env.DISCORD_CHANNEL_ID,

      // Behavior
      respondToMentions: true,
      postThoughts: true,
      thoughtChannelId: "channel-for-thoughts",
    }),
  ],
});
```

**Features:**
- Post to channels
- Respond to mentions
- Slash commands
- Reaction handling
- Role-based permissions

---

### @ink/plugin-telegram

Telegram bot for direct user interaction.

```bash
npm install @ink/plugin-telegram
```

```typescript
import { telegramPlugin } from "@ink/plugin-telegram";

const agent = new Agent({
  plugins: [
    telegramPlugin({
      token: process.env.TELEGRAM_BOT_TOKEN,
      chatId: process.env.TELEGRAM_CHAT_ID,

      // Who can interact
      allowedUsers: ["user1", "user2"],
      adminUsers: ["admin1"],
    }),
  ],
});
```

## Data Plugins

### @ink/plugin-news

Multi-source news aggregation with smart filtering.

```bash
npm install @ink/plugin-news
```

```typescript
import { newsPlugin } from "@ink/plugin-news";

const agent = new Agent({
  plugins: [
    newsPlugin({
      sources: [
        { name: "coindesk", url: "https://coindesk.com/rss" },
        { name: "techcrunch", url: "https://techcrunch.com/feed" },
        { name: "hackernews", type: "hackernews", minScore: 100 },
      ],

      // Filtering
      keywords: ["ethereum", "ai", "crypto"],
      excludeKeywords: ["sponsored", "advertisement"],

      // Schedule
      refreshIntervalMinutes: 15,
      maxItemsPerSource: 10,

      // Processing
      generateEmbeddings: true,
      extractEntities: true,
    }),
  ],
});
```

**Features:**
- RSS/Atom feed support
- Hacker News integration
- Reddit subreddit monitoring
- Keyword filtering
- Automatic embedding generation
- Entity extraction

---

### @ink/plugin-markets

Real-time market data integration.

```bash
npm install @ink/plugin-markets
```

```typescript
import { marketsPlugin } from "@ink/plugin-markets";

const agent = new Agent({
  plugins: [
    marketsPlugin({
      // Crypto markets
      crypto: {
        provider: "coingecko",
        symbols: ["BTC", "ETH", "SOL"],
        refreshMinutes: 5,
      },

      // Stock markets
      stocks: {
        provider: "polygon",
        apiKey: process.env.POLYGON_API_KEY,
        symbols: ["AAPL", "MSFT", "NVDA"],
      },

      // Prediction markets
      predictions: {
        provider: "polymarket",
        categories: ["politics", "crypto", "tech"],
      },
    }),
  ],
});
```

**Features:**
- Real-time price data
- Historical data
- Market sentiment
- Prediction market integration

## Action Plugins

### @ink/plugin-trading

Paper and live trading capabilities.

```bash
npm install @ink/plugin-trading
```

```typescript
import { tradingPlugin } from "@ink/plugin-trading";

const agent = new Agent({
  plugins: [
    tradingPlugin({
      // Paper trading (safe)
      mode: "paper",
      initialBalance: 10000,

      // Or live trading (be careful!)
      // mode: "live",
      // exchange: "binance",
      // apiKey: process.env.BINANCE_API_KEY,
      // apiSecret: process.env.BINANCE_API_SECRET,

      // Risk management
      maxPositionSize: 0.1,  // 10% of portfolio
      stopLossPercent: 5,
      takeProfitPercent: 15,

      // Behavior
      autoTrade: false,  // Require manual confirmation
      requireConfidence: 0.8,
    }),
  ],
});
```

**Actions:**
- `trading.buy(symbol, amount, options)`
- `trading.sell(symbol, amount, options)`
- `trading.getPortfolio()`
- `trading.getHistory()`
- `trading.cancelOrder(orderId)`

---

### @ink/plugin-images

AI image generation for visual content.

```bash
npm install @ink/plugin-images
```

```typescript
import { imagesPlugin } from "@ink/plugin-images";

const agent = new Agent({
  plugins: [
    imagesPlugin({
      provider: "replicate",
      apiKey: process.env.REPLICATE_API_KEY,
      model: "stability-ai/sdxl",

      // Defaults
      defaultWidth: 1024,
      defaultHeight: 1024,

      // Style
      stylePrefix: "digital art, vibrant colors,",
      negativePrompt: "blurry, low quality",
    }),
  ],
});
```

**Actions:**
- `images.generate(prompt, options)`
- `images.upscale(imageUrl)`
- `images.variations(imageUrl)`

## Utility Plugins

### @ink/plugin-scheduler

Advanced scheduling beyond crons.

```bash
npm install @ink/plugin-scheduler
```

```typescript
import { schedulerPlugin } from "@ink/plugin-scheduler";

const agent = new Agent({
  plugins: [
    schedulerPlugin({
      tasks: [
        {
          name: "morning-briefing",
          schedule: "0 9 * * *",  // 9 AM daily
          action: async (ctx) => {
            await ctx.action("twitter.post", "Good morning!");
          },
        },
        {
          name: "market-update",
          schedule: "0 */4 * * *",  // Every 4 hours
          action: async (ctx) => {
            const markets = await ctx.action("markets.getSummary");
            // Process and post...
          },
        },
      ],
    }),
  ],
});
```

---

### @ink/plugin-webhooks

Receive external webhooks.

```bash
npm install @ink/plugin-webhooks
```

```typescript
import { webhooksPlugin } from "@ink/plugin-webhooks";

const agent = new Agent({
  plugins: [
    webhooksPlugin({
      secret: process.env.WEBHOOK_SECRET,
      handlers: {
        "github/push": async (payload, ctx) => {
          await ctx.remember({
            type: "episodic",
            content: `New commit: ${payload.commits[0].message}`,
          });
        },
        "stripe/payment": async (payload, ctx) => {
          // Handle payment events
        },
      },
    }),
  ],
});
```

## Installation

All official plugins follow the same pattern:

```bash
# Install the plugin
npm install @ink/plugin-NAME

# Import and configure
import { NAMEPlugin } from "@ink/plugin-NAME";

const agent = new Agent({
  plugins: [NAMEPlugin({ ...config })],
});
```

## Support

Official plugins are supported through:

- GitHub Issues on each plugin repo
- Discord #plugins channel
- Documentation at docs.ink.dev/plugins

## Next Steps

<CardGroup cols={2}>
  <Card title="Community Plugins" icon="users" href="/plugins/community">
    Explore community-built plugins.
  </Card>
  <Card title="Build Your Own" icon="hammer" href="/guides/custom-plugins">
    Create custom plugins.
  </Card>
</CardGroup>
